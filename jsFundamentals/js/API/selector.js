(function() {
  'use strict';

  // The Selector API can select css selectors from the DOM. Elements, Fragments and Docuemnts.

  // querySelector will find the firt instance of the css selector you pass it.

  var heading = document.querySelector('header :first-child');
  // here we find the first el in the header element.
  // also simple css rules can be found.


  // querySelectorAll - will find all with the name passed

}());
